# To create vue js project
    - npx create-vue project-name

# Steps to run
    - docker-compose build
    - docker-compose up -d

# To run at once
    - docker-compose up -d --build

# To stop and remove docker container
    - docker-compose down